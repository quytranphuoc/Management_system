# run smart contract

npx hardhat node

npx hardhat run scripts/deploy2.js --network localhost

khi chạy xong tiếp

npx hardhat compile để hiển thị ra file json ở artifacts/contracts/

# run frontend

npm run dev

# run backend

run npm
